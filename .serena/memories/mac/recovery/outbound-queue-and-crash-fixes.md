macOS now queues encrypted outbound envelopes in `OutboundEnvelopeQueueStore` under Application Support and drains them from `AppContainer` after relay reconnect or app relaunch. Recent crash fixes removed `Bundle.module` from the packaged app path, removed the paired-menu `TimelineView` from `StatusMenuView`, and removed the `@MainActor` isolation from `LocalNotificationGateway.apply` so UNUserNotificationCenter completion handlers do not trigger dispatch queue assertion crashes.