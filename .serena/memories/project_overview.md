air-bridge is a personal Mac <-> Android bridge focused on clipboard sync and Android notification mirroring with server-blind end-to-end encryption. Repo structure is top-level android/, mac/, and server/. Current priority is relay server first, then Android, then macOS refinement. Android code is not implemented yet; architecture docs define pairing, notification listener, clipboard sync, secure key/token storage, and WebSocket/HTTP relay integration.